---
title: "Development Workflow"
description: "Процессы разработки, тестирования и CI/CD"
tags: ["development", "testing", "workflow", "devtools"]
version: "3.1"
---

# Development Workflow

## 🎯 High-Level Overview

Быстрая разработка с поддержкой:
- Docker-based workflow (пересборка за 30 сек)
- Локальная разработка (без Docker)
- Helper скрипт `dev.sh`
- Автоматическое тестирование

## 🔧 Helper Script: dev.sh

**Расположение:** `scripts/utils/dev.sh`

### Docker Commands

```bash
cd /home/ilyasni/n8n-server/n8n-installer/telethon

# Пересборка + перезапуск (основной workflow)
./scripts/utils/dev.sh rebuild

# Рестарт без пересборки
./scripts/utils/dev.sh restart

# Просмотр логов
./scripts/utils/dev.sh logs

# Остановка
./scripts/utils/dev.sh stop

# Bash внутри контейнера
./scripts/utils/dev.sh shell
```

### Local Development

```bash
# Первоначальная настройка (один раз)
./scripts/utils/dev.sh setup

# Запуск всей системы локально
./scripts/utils/dev.sh local

# Только FastAPI сервер
./scripts/utils/dev.sh api

# Только Telegram бот
./scripts/utils/dev.sh bot

# Запуск тестов
./scripts/utils/dev.sh test
```

### Алиасы для быстрого доступа

**Добавьте в `~/.bashrc`:**

```bash
# Telethon Development Aliases
TELETHON_PATH="/home/ilyasni/n8n-server/n8n-installer/telethon"

alias telethon-rebuild="$TELETHON_PATH/scripts/utils/dev.sh rebuild"
alias telethon-restart="$TELETHON_PATH/scripts/utils/dev.sh restart"
alias telethon-logs="$TELETHON_PATH/scripts/utils/dev.sh logs"
alias telethon-stop="$TELETHON_PATH/scripts/utils/dev.sh stop"
alias telethon-shell="$TELETHON_PATH/scripts/utils/dev.sh shell"
alias telethon-local="$TELETHON_PATH/scripts/utils/dev.sh local"
alias telethon-test="$TELETHON_PATH/scripts/utils/dev.sh test"
alias telethon-cd="cd $TELETHON_PATH"
alias telethon-help="$TELETHON_PATH/scripts/utils/dev.sh help"
```

**Применить:**
```bash
source ~/.bashrc

# Теперь из любой директории:
telethon-rebuild  # Пересобрать
telethon-logs     # Посмотреть логи
telethon-cd       # Перейти в директорию
```

## 🔄 Development Cycle

### Типичный цикл с Docker

```bash
# 0. ОБЯЗАТЕЛЬНО: Изучить документацию через Context7
# → Context7: "Feature name best practices"
# → Context7: "Library name API documentation"

# 1. Внести изменения
vim telethon/qr_auth_manager.py

# 2. Пересобрать и запустить
telethon-rebuild

# 3. Смотреть логи
telethon-logs

# 4. Тестировать через API/бота
curl http://localhost:8010/docs

# 5. При необходимости войти в контейнер
telethon-shell

# 6. Повторить цикл
```

### Context7 Workflow (ОБЯЗАТЕЛЬНО)

**Перед началом любой задачи:**

```bash
# 1. Определите технологию/библиотеку
# Telegram Bot API, FastAPI, SQLAlchemy, Qdrant, etc.

# 2. Используйте Context7 для изучения
Context7: "Technology name best practices"
Context7: "Technology name current API version"
Context7: "Technology name common patterns"

# 3. Найдите примеры
Context7: "Technology name example code"
Context7: "Technology name async patterns"  # для async code

# 4. Проверьте актуальность
# Убедитесь что версия в документации совпадает с вашей

# 5. ТОЛЬКО ПОТОМ пишите код
```

**Примеры для разных задач:**

```bash
# Добавление Telegram Mini App
→ Context7: "Telegram Mini Apps API documentation"
→ Context7: "Telegram WebAppInfo best practices"
→ Context7: "Telegram Mini Apps dark theme support"

# Работа с FastAPI
→ Context7: "FastAPI background tasks async"
→ Context7: "FastAPI dependency injection patterns"
→ Context7: "FastAPI middleware CORS configuration"

# SQLAlchemy модели
→ Context7: "SQLAlchemy relationships many-to-many"
→ Context7: "SQLAlchemy timezone aware datetime PostgreSQL"
→ Context7: "SQLAlchemy async queries best practices"

# Интеграция Qdrant
→ Context7: "Qdrant Python client async examples"
→ Context7: "Qdrant vector search filters"
→ Context7: "Qdrant batch operations performance"
```

### Локальная разработка (без Docker)

**Преимущества:** мгновенные изменения, нет пересборки

```bash
# Первоначальная настройка (один раз)
telethon-cd
./scripts/utils/dev.sh setup

# Создаст:
# - venv/
# - Установит dependencies
# - Скопирует .env

# Запуск локально
./scripts/utils/dev.sh local

# Изменения применяются сразу после Ctrl+C и повторного запуска
```

## 🧪 Testing

### Структура тестов

```
tests/
├── test_models.py              # SQLAlchemy модели
├── test_many_to_many.py        # Relationships
├── test_retention_system.py    # Cleanup service
├── test_qr_auth.py             # QR Login
├── test_admin_panel.py         # Admin API
├── test_rag_pipeline.py        # RAG integration
├── test_evaluation_schemas.py  # Evaluation data models
├── test_golden_dataset_manager.py  # Dataset management
├── test_bot_evaluator.py       # LLM evaluation logic
└── test_evaluation_runner.py   # Batch evaluation orchestration
```

### Запуск тестов

```bash
# Все тесты
telethon-test

# Конкретный файл
pytest tests/test_qr_auth.py -v

# С coverage
pytest tests/ --cov=. --cov-report=html

# Только быстрые
pytest -m "not slow" tests/

# Evaluation тесты
pytest tests/test_evaluation_*.py -v

# С coverage для evaluation
pytest tests/test_evaluation_*.py --cov=evaluation --cov-report=html
```

### Пример теста

```python
# tests/test_qr_auth.py
import pytest
from unittest.mock import AsyncMock, patch
from datetime import datetime, timezone

@pytest.mark.asyncio
async def test_qr_session_creation():
    """Тест создания QR session"""
    # Mock Redis
    with patch('redis.Redis') as mock_redis:
        mock_redis_instance = mock_redis.return_value
        mock_redis_instance.setex.return_value = True
        
        # Create session
        session_id = await qr_auth_manager.create_qr_session(
            telegram_id=123,
            invite_code="ABC123"
        )
        
        # Verify
        assert session_id is not None
        assert len(session_id) == 36  # UUID format
        
        # Verify Redis call
        mock_redis_instance.setex.assert_called_once()
        call_args = mock_redis_instance.setex.call_args
        assert call_args[0][1] == 600  # TTL 10 минут

@pytest.mark.asyncio
async def test_qr_session_expiry():
    """Тест истечения QR session"""
    # Setup expired session
    session_id = "test-session-123"
    redis_client.setex(
        f"qr_session:{session_id}",
        1,  # 1 секунда
        json.dumps({"status": "waiting"})
    )
    
    # Wait for expiry
    await asyncio.sleep(2)
    
    # Verify
    session = await qr_auth_manager.get_qr_session(session_id)
    assert session is None  # Expired
```

### Пример теста для Evaluation системы

```python
# tests/test_evaluation_schemas.py
import pytest
from datetime import datetime, timezone
from evaluation.schemas import GoldenDatasetItem, TelegramContext, ContextType, DifficultyLevel, ToneType

def test_golden_dataset_item_creation():
    """Тест создания GoldenDatasetItem"""
    # Arrange
    telegram_context = TelegramContext(
        user_id=12345,
        channels=["automotive_news"],
        context_type=ContextType.SINGLE_CHANNEL
    )
    
    # Act
    item = GoldenDatasetItem(
        item_id="test_001",
        dataset_name="test_dataset",
        category="automotive",
        input={"message_text": "Test query"},
        query="Test query",
        telegram_context=telegram_context,
        expected_output="Expected response",
        difficulty=DifficultyLevel.BEGINNER,
        tone=ToneType.TECHNICAL,
        requires_multi_source=False
    )
    
    # Assert
    assert item.item_id == "test_001"
    assert item.telegram_context.user_id == 12345
    assert item.difficulty == DifficultyLevel.BEGINNER
    assert item.tone == ToneType.TECHNICAL
    assert item.created_at.tzinfo is not None  # timezone-aware

@pytest.mark.asyncio
async def test_golden_dataset_manager_crud():
    """Тест CRUD операций для GoldenDatasetManager"""
    from evaluation.golden_dataset_manager import GoldenDatasetManager
    from unittest.mock import AsyncMock
    
    # Mock asyncpg pool
    mock_pool = AsyncMock()
    mock_conn = AsyncMock()
    mock_pool.acquire.return_value.__aenter__.return_value = mock_conn
    
    manager = GoldenDatasetManager(mock_pool)
    
    # Test add_item
    item = GoldenDatasetItem(
        item_id="test_001",
        dataset_name="test_dataset",
        category="automotive",
        input={"message_text": "Test"},
        query="Test query",
        telegram_context=TelegramContext(
            user_id=12345,
            channels=["test_channel"],
            context_type=ContextType.SINGLE_CHANNEL
        ),
        expected_output="Test response"
    )
    
    await manager.add_item(item)
    
    # Verify database call
    mock_conn.execute.assert_called_once()
    
    # Test get_dataset_stats
    mock_conn.fetchrow.return_value = {"total": 10, "categories": {"automotive": 5}}
    stats = await manager.get_dataset_stats("test_dataset")
    
    assert stats["total"] == 10
    assert stats["categories"]["automotive"] == 5

@pytest.mark.asyncio
async def test_bot_evaluator_metrics():
    """Тест BotEvaluator с мокированием Ragas"""
    from evaluation.bot_evaluator import BotEvaluator
    from unittest.mock import AsyncMock, patch
    
    # Mock LLM
    mock_llm = AsyncMock()
    
    # Mock Ragas metrics
    with patch('evaluation.bot_evaluator.RAGAS_AVAILABLE', True):
        with patch('evaluation.bot_evaluator.AnswerCorrectness') as mock_metric:
            evaluator = BotEvaluator(
                model_provider="openrouter",
                model_name="gpt-4o-mini",
                evaluator_llm=mock_llm
            )
            
            # Test evaluation
            mock_metric.return_value.ascore.return_value = 0.85
            
            result = await evaluator.evaluate_item(
                item_id="test_001",
                query="Test query",
                context=["Test context"],
                response="Test response",
                reference_answer="Reference answer"
            )
            
            assert result["overall_score"] == 0.85
            assert "answer_correctness" in result["scores"]
```

## 📝 Linting & Formatting

### Ruff (линтер)

```bash
# Проверка
ruff check .

# Автофикс
ruff check . --fix

# Конфигурация в pyproject.toml:
[tool.ruff]
line-length = 100
target-version = "py311"
```

### Type Checking

```bash
# mypy
mypy telethon/ --ignore-missing-imports

# pyright
pyright telethon/
```

## 📊 Monitoring Logs

### Docker Logs

```bash
# Последние 50 строк
docker logs telethon --tail 50
docker logs telethon-bot --tail 50

# Live логи
docker logs -f telethon

# За последние 10 минут
docker logs --since 10m telethon

# Поиск в логах
docker logs telethon 2>&1 | grep "ERROR"
docker logs telethon 2>&1 | grep "QRAuthManager"
```

### Типичные логи при успешной работе

```
✅ База данных инициализирована
✅ FastAPI запущен на http://0.0.0.0:8010
✅ Parser Service запущен (интервал: 30 минут)
✅ Cleanup Service запущен (очистка в 03:00)
✅ Telegram Bot запущен
✅ QRAuthManager подключен к Redis
🔄 ParserService: Парсинг 1 каналов для пользователя...
✅ ParserService: @channel_name - добавлено 2 постов
🏷️ TaggingService: Начинаем обработку 2 постов
✅ TaggingService: Сгенерировано 5 тегов
```

## 🐛 Debugging

### Войти в контейнер

```bash
# Bash в telethon
telethon-shell

# Python REPL
docker exec -it telethon python

# Проверка БД
docker exec -it supabase-db psql -U postgres -d postgres

# Проверка Redis
docker exec -it redis redis-cli
```

### Debug режим

```python
# main.py
import logging

# DEBUG логи
logging.basicConfig(level=logging.DEBUG)

# SQLAlchemy queries
engine = create_engine(
    database_url,
    echo=True  # Показывает SQL запросы
)
```

## 🔄 Git Workflow

### Branch Strategy

```bash
# Feature branch
git checkout -b feature/qr-login-improvements

# Bug fix
git checkout -b fix/timezone-handling

# Documentation
git checkout -b docs/update-admin-guide
```

### Commit Messages

```bash
# ✅ Правильно - осмысленные коммиты
git commit -m "feat(qr-auth): Add QR session expiry check"
git commit -m "fix(parser): Handle timezone-aware datetime"
git commit -m "docs(admin): Update Admin Panel quickstart"
git commit -m "test(rag): Add RAG pipeline integration test"

# ❌ Неправильно
git commit -m "fix"
git commit -m "updated code"
git commit -m "changes"
```

### Pre-commit Checks

```bash
# Перед коммитом:

# 1. Линтер
ruff check .

# 2. Тесты
pytest tests/ -v

# 3. Type check
mypy telethon/

# 4. Проверка что нет чувствительных данных
git diff --cached | grep -i "password\|secret\|token"
```

## ✅ Verification Steps

1. **dev.sh работает:**
```bash
./scripts/utils/dev.sh help
# Должен показать help message
```

2. **Docker rebuild успешен:**
```bash
telethon-rebuild
docker ps | grep telethon
# Контейнеры запущены
```

3. **Тесты проходят:**
```bash
telethon-test
# All tests passed
```

4. **Логи не содержат ошибок:**
```bash
telethon-logs | grep ERROR
# Пустой output
```

5. **Линтер чистый:**
```bash
ruff check .
# All checks passed
```

## 🚨 Troubleshooting

### Контейнеры не запускаются

```bash
# Проверить статус
docker ps -a | grep telethon

# Просмотреть ошибки
docker logs telethon
docker logs telethon-bot

# Пересобрать с нуля
telethon-stop
docker rm telethon telethon-bot
telethon-rebuild
```

### Изменения не применяются

```bash
# Обязательно пересобирайте!
telethon-rebuild  # Не просто restart!

# Проверьте что volumes смонтированы
docker inspect telethon | grep -A 10 Mounts
```

### Тесты падают

```bash
# Проверьте environment
pytest tests/ -v -s  # -s показывает print

# Изолированный тест
pytest tests/test_qr_auth.py::test_qr_session_creation -v

# Debug mode
pytest tests/ --pdb  # Откроет debugger на ошибке
```

## 📚 Связанные правила

- `01-core.mdc` — Основные правила
- `02-architecture.mdc` — Docker setup
- `08-api.mdc` — API testing
